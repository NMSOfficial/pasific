import type { CefrLevel } from '../types/entities';

/** Single source of truth for the level scale, used by every level filter/picker in the app. */
export const CEFR_LEVELS: CefrLevel[] = ['B1', 'B1+', 'B2', 'B2+', 'C1'];
